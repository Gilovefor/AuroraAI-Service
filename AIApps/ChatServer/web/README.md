# ChatServer Web (Vue 3 + Vite)

## 开发

```bash
cd AIApps/ChatServer/web
npm install
npm run dev
```

可通过 `VITE_API_TARGET` 指定后端地址，例如：

```bash
VITE_API_TARGET=http://127.0.0.1:8080 npm run dev
```

## 构建

```bash
npm run build
```

构建产物输出到：`AIApps/ChatServer/resource/dist`。
